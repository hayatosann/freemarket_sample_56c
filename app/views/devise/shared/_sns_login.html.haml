- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to "Facebookでログイン", omniauth_authorize_path(resource_name, provider), style: "color: white"
    %br/